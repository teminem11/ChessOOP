# Chess OOP

Console chess game in Java with an object-oriented board and piece model. It supports legal movement and captures, alternating turns, check protection, checkmate/stalemate detection, and automatic queen promotion. Enter moves as `e2 e4`.

```bash
mvn package
java -cp target/classes Main
```
